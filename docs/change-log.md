# Change Log

## 2026-04-22 - Knowledge Base Study Space And Checklists

- Что изменилось:
  - в базу знаний добавлено третье изолированное пространство `учёба`;
  - `/cards` и `/cards/new` теперь строят выбор пространств из единой доменной конфигурации `life/work/study`;
  - модель карточки расширена режимом `contentType` и структурированным `checklist`;
  - создание, редактирование, просмотр, preview и поиск карточек учитывают checklist-содержимое;
  - добавлены integration и e2e тесты для `study` workspace и создания карточки-чек-листа.
- Зачем:
  - отделить учебные знания от личной и рабочей баз;
  - сделать карточки пригодными не только для заметок, но и для списков действий/проверок.
- Статус: `accepted`

## 2026-03-16 - Planner and Study Flow Stabilization

- Что изменилось:
  - упрощены home cards;
  - перестроен workflow language course;
  - исправлены возвраты и навигация в planner;
  - убраны отдельные production blockers и lazy-loading проблемы.
- Зачем:
  - стабилизировать основные пользовательские переходы;
  - упростить стартовый экран;
  - подготовить базу для дальнейшей перестройки учебного модуля.
- Статус: `accepted`

## 2026-03-17 - Languages Workspace Experiments

- Что изменилось:
  - добавлены workspace и deploy fixes для language flow;
  - добавлена Ollama support для языкового AI.
- Зачем:
  - проверить отдельный сценарий языкового обучения и его AI-поддержку.
- Статус: `reworked`

## 2026-03-18 - Removal of Old Language Workspace

- Что изменилось:
  - OpenAI убран из language workspace;
  - сам language learning workspace затем удалён.
- Зачем:
  - свернуть неудачную отдельную ветку и вернуть фокус в основной продукт.
- Статус: `accepted`

## 2026-03-18 - Study Infrastructure Added

- Что изменилось:
  - добавлена инфраструктура учебников.
- Зачем:
  - создать основу для отдельного режима работы с учебниками.
- Статус: `accepted`

## 2026-03-19 - Article Card Type

- Что изменилось:
  - добавлен тип карточки article.
- Зачем:
  - расширить knowledge-base сценарии.
- Статус: `accepted`

## 2026-03-21 - Interactive Study Blocks

- Что изменилось:
  - study lessons были переработаны в interactive blocks;
  - добавлена поддержка custom study block type.
- Зачем:
  - перейти от плоского представления материала к более управляемым структурным блокам.
- Статус: `accepted`

## 2026-03-22 - Planner Layout Iteration

- Что изменилось:
  - обновлён layout planner;
  - улучшено редактирование заголовка;
  - folders переведены на 3-column grid.
- Зачем:
  - улучшить визуальную управляемость и плотность интерфейса планировщика.
- Статус: `accepted`

## 2026-03-23 - Planner Deadlines and Ordering

- Что изменилось:
  - добавлено редактирование period;
  - исправлен edit calendar layout;
  - добавлены deadlines и notifications;
  - добавлена сортировка по deadline и порядок folders;
  - folders переведены на drag reorder.
- Зачем:
  - сделать planner реальным инструментом ежедневной работы, а не только списком задач.
- Статус: `accepted`

## 2026-03-23 - Editor Scale Adjustment

- Что изменилось:
  - увеличен display scale текстового редактора.
- Зачем:
  - улучшить читаемость и удобство работы в editor/texts.
- Статус: `accepted`

## 2026-03-24 - Knowledge Folders

- Что изменилось:
  - карточки сгруппированы по сферам;
  - сферы превращены в knowledge folders.
- Зачем:
  - сделать структуру базы знаний более навигационной и удобной для роста.
- Статус: `accepted`

## 2026-03-26 - Daily Plans Collection

- Что изменилось:
  - добавлен сбор daily deadline plans;
  - исправлена загрузка detail page;
  - сохранены исходные планы при сборке;
  - добавлено обновление stale tasks;
  - убрано создание пустых daily plans;
  - отсутствующие daily plans редиректятся в planner.
- Зачем:
  - сформировать устойчивую модель дневных планов как производных от общих планов.
- Статус: `accepted`

## 2026-03-29 - Production Workflow and New Study UI

- Что изменилось:
  - добавлен стабильный Vercel production deploy workflow;
  - в remote history появился новый study parsing UI на базе Gemini;
  - затем `study 2.0` был заново собран как interactive textbook.
- Зачем:
  - улучшить production delivery;
  - попробовать новую ветку UI для учебников.
- Статус: `reworked`

## 2026-04-05 - Study 2.0 Eliminated

- Что изменилось:
  - удалён код `study 2.0`;
  - сохранены редиректы со старых `/study` маршрутов на `study-3` и `/cards`;
  - добавлен одноразовый cleanup legacy study-данных после деплоя;
  - `npm run lint` переведён на batched runner через `scripts/run-eslint.mjs`;
  - production deploy был успешно обновлён.
- Зачем:
  - окончательно закрыть legacy study-ветку и оставить единую реализацию учебников;
  - сделать деплой и сопровождение проекта более стабильными.
- Статус: `accepted`

## 2026-04-05 - Remote Main History Integrated

- Что изменилось:
  - удалённая история `main` была безопасно интегрирована без force-push;
  - итоговое состояние отправлено в GitHub через обычный push.
- Зачем:
  - синхронизировать локальную и удалённую линии разработки, не переписывая remote history.
- Статус: `accepted`

## 2026-04-05 - Agent Docs Bootstrap and Git Workflow Stabilized

- Что изменилось:
  - добавлена папка `docs/` с единым стартовым prompt, картой проекта, coding rules, current state, changelog, roadmap и open issues;
  - локальный `main` заново выровнен с актуальным `origin/main`;
  - broken refs вида `main 2` удалены.
- Зачем:
  - дать следующим ИИ-агентам устойчивую стартовую точку;
  - вернуть локальному git workflow предсказуемость для обычных `pull` и `push`.
- Статус: `accepted`

## 2026-04-05 - Planner Subtasks Across General And Daily Plans

- Что изменилось:
  - задачи в planner получили вложенные подзадачи;
  - для подзадач добавлены редактирование, дедлайны, history, удаление и отдельное завершение;
  - добавлена сортировка подзадач по дедлайнам внутри родительской задачи;
  - detail page плана была перестроена на рекурсивный task UI;
  - синхронизация между общими и дневными планами стала переносить не только статус задачи, но и всё дерево подзадач.
- Зачем:
  - дать возможность дробить задачи внутри planner без потери общей структуры;
  - сохранить единое состояние задач и подзадач во всех производных дневных планах.
- Статус: `accepted`

## 2026-04-05 - Knowledge Base Split Into Life And Work

- Что изменилось:
  - у карточек базы знаний появилось обязательное поле workspace с разделением на `жизнь` и `работа`;
  - legacy карточки без workspace автоматически нормализуются в `жизнь`;
  - экран `/cards` превращён в выбор пространства, а `/cards/new` теперь сначала предлагает выбрать workspace;
  - добавлен отдельный маршрут списка карточек внутри пространства;
  - поиск, сферы, теги, просмотр и редактирование карточек теперь работают в контексте выбранного workspace.
- Зачем:
  - изолировать личную и рабочую базы знаний друг от друга;
  - исключить смешанные списки и выдачи между двумя пространствами.
- Статус: `accepted`

## 2026-04-05 - Automated Test Infrastructure And Critical Flows

- Что изменилось:
  - добавлены конфигурации `Playwright` и `Vitest`;
  - добавлены `tests/e2e`, `tests/integration`, фикстуры, helpers и global setup/teardown;
  - добавлены test reset/seed scripts и изолированное test storage через `TEST_DATA_ROOT`;
  - добавлены smoke и critical flow тесты для auth, planner, cards workspace, study-3, texts и editor;
  - добавлен CI workflow `.github/workflows/tests.yml`.
- Зачем:
  - начать ловить регрессии фронтенда и route/store логики автоматически;
  - стабилизировать локальный и CI запуск тестов вокруг основных продуктовых сценариев.
- Статус: `accepted`

## 2026-04-06 - Study 3 Vocabulary Split From Knowledge Cards

- Что изменилось:
  - `study-3` перестал сохранять слова в `/cards` и начал писать их в отдельный `vocabulary`-домен;
  - внутри `Учебники 3.0` добавлены страницы `словарь` и `учить лексику`;
  - для словаря добавлены последовательный первый проход и упрощённые интервальные повторения;
  - добавлены integration и e2e тесты для новых vocabulary-сценариев;
  - production deploy обновлён до этой версии.
- Зачем:
  - отделить лексику учебников от базы знаний;
  - подготовить отдельную архитектуру карточек, которую можно дальше усложнять без влияния на knowledge cards.
- Статус: `accepted`

## 2026-04-06 - Mnemonic Layer For Study 3 Vocabulary Cards

- Что изменилось:
  - у lexical cards `study-3` добавлен встроенный слой мнемотехники со статусами `none`, `in_progress`, `anchored`;
  - добавлены структурированные режимы `sound`, `image`, `linked_word`, `chain`, `nested`;
  - добавлен отдельный API route для чтения и обновления mnemonic-state карточки;
  - в review UI добавлены ручное включение мнемотехники, рекомендации после ошибок и нестабильных ответов, а также быстрая проверка, сработала ли связь;
  - добавлены integration и e2e тесты для mnemonic flow.
- Зачем:
  - помочь пользователю быстро собирать простые связи для слабых слов, не превращая словарь в отдельную сложную систему;
  - сохранить мнемотехнику вспомогательным режимом внутри существующих карточек и повторений.
- Статус: `accepted`

## 2026-04-06 - Study 3 Deletion Flows And Reader Rebalance

## 2026-04-20 - Sound Color Exercise Modes In Standalone Prototype

- Что изменилось:
  - standalone prototype `звук -> цвет` был перестроен из разрозненных упражнений в stage/phase pipeline;
  - добавлены этапы `ASSIGNMENT`, `NOTES_IN_ONE_OCTAVE`, `INTERVALS`, `MELODY_IN_ONE_OCTAVE`, `OCTAVES`;
  - для этапов 1–4 добавлены отдельные подфазы `fixation` и `test`;
  - добавлен единый registry режимов с subset-логикой и ограничением ответов текущим набором нот/цветов;
  - для octave-stage добавлена `getColorForOctave(baseColor, octaveOffset)`.
- Зачем:
  - убрать размытую логику и развести assignment, fixation и test как отдельные части учебного pipeline;
  - подготовить архитектуру для роста от одиночных нот к интервалам, мелодиям и октавам.
- Статус: `accepted`

- Что изменилось:
  - в библиотеке `study-3` добавлено удаление учебников и отдельных image-based page entries;
  - в словаре `study-3` добавлено удаление lexical cards;
  - в mnemonic flow добавлено удаление мнемотехники целиком и удаление отдельных внутренних элементов;
  - reader `study-3` перестроен в двухколоночный рабочий layout с равными desktop-колонками для страницы/HTML и ассистента;
  - добавлены integration и e2e тесты для новых delete-flow сценариев и reader layout.
- Зачем:
  - дать пользователю прямой контроль над загруженными материалами, карточками и мнемотехническими дополнениями;
  - сделать рабочую страницу учебника визуально более пригодной для параллельной работы с HTML и ассистентом.
- Статус: `accepted`

## 2026-04-06 - Main Synced And Production Updated

- Что изменилось:
  - текущее состояние репозитория было зафиксировано в `main` и отправлено в `origin/main`;
  - production deploy обновлён до версии, где уже присутствуют vocabulary split, mnemonic-layer, delete-flows и reader rebalance.
- Зачем:
  - синхронизировать удалённый `main` с фактическим локальным состоянием проекта;
  - закрепить на production актуальную реализацию `study-3` и словаря.
- Статус: `accepted`

## 2026-04-06 - Mind Palaces MVP

- Что изменилось:
  - добавлен отдельный домен `Чертоги разума` с собственными маршрутами, API, domain-логикой и persistence-слоем;
  - реализованы создание чертога, фиксация линейного маршрута из 5–15 точек и обязательная проверка вперёд, назад и по номеру;
  - доменная валидация стала отклонять абстрактные точки, пропуски, прыжки и попытки стабилизировать маршрут до прохождения всех проверок;
  - на главную страницу и в верхнюю навигацию добавлен вход в новый модуль;
  - добавлены integration/domain/API тесты для route-инвариантов и проверочного цикла.
- Зачем:
  - выделить `Чертоги разума` в отдельную систему, которая помогает пользователю строить устойчивый маршрут памяти, а не смешивает этот сценарий с карточками или словарём;
  - закрепить в коде когнитивные ограничения ТЗ как обязательные продуктовые правила.
- Статус: `accepted`

## 2026-04-06 - Mind Palaces Deployed To Production

- Что изменилось:
  - коммит `Add mind palaces MVP` был отправлен в `origin/main`;
  - production deploy на Vercel был обновлён до версии с модулем `Чертоги разума`;
  - production-домен `memory-system-delta.vercel.app` продолжил отвечать через стандартный login redirect.
- Зачем:
  - зафиксировать, что новый модуль уже не только реализован локально, но и опубликован в production;
  - синхронизировать документацию с фактическим состоянием deploy-среды.
- Статус: `accepted`

## 2026-04-06 - Study 3 Persistent HTML Page State

- Что изменилось:
  - HTML pipeline `study-3` переведён с клиентского временного cache-first поведения на persistent page-state в server store;
  - для каждой страницы введены `html_content` и статус `not_generated | generated`;
  - route [`app/api/study-3/books/[bookId]/html/route.ts`](../app/api/study-3/books/[bookId]/html/route.ts) теперь разделяет чтение сохранённого HTML через `GET` и инкрементальную генерацию через `POST`;
  - reader получил отдельные кнопки `Получить HTML` и `Показать HTML`;
  - добавлены integration-тесты на новый HTML route contract и обновлён reader smoke.
- Зачем:
  - исключить повторную генерацию уже обработанных страниц;
  - сделать сохранённый HTML единым источником истины;
  - явно разделить в UI генерацию и просмотр.
- Статус: `accepted`

## 2026-04-06 - Study 3 Assistant Prompt Unbound From Page Fragment

- Что изменилось:
  - prompt route [`app/api/study-3/assistant/chat/route.ts`](../app/api/study-3/assistant/chat/route.ts) больше не требует отвечать только в пределах выбранного фрагмента и ближайшего контекста;
  - выбранная фраза и контекст сохранены как вспомогательная подсказка, а не как жёсткое ограничение;
  - reader UI теперь прямо сообщает, что ассистенту можно задавать любые вопросы по языку.
- Зачем:
  - дать пользователю возможность переходить от разбора конкретной фразы к общим языковым вопросам без смены экрана;
  - убрать искусственный отказ ассистента на вопросы шире текущего HTML-фрагмента.
- Статус: `accepted`

## 2026-04-06 - Study 3 Layout JSON Source Of Truth

- Что изменилось:
  - HTML pipeline `study-3` переведён на JSON-first flow: Gemini теперь сначала запрашивается на structured `layout_json`, а HTML рендерится сервером;
  - в persistent page-state добавлено поле `layout_json`, а `html_content` стал производным render artifact;
  - добавлен [`lib/server/study-3-renderer.ts`](../lib/server/study-3-renderer.ts) для преобразования сохранённой структуры страницы в HTML;
  - добавлены integration-тесты на layout-contract route/store flow, и весь integration suite снова приведён к зелёному состоянию.
- Зачем:
  - сделать HTML более управляемым и пригодным для сохранения визуальной структуры учебника без жёсткой зависимости от raw HTML Gemini;
  - подготовить основу для дальнейшей block-based реконструкции страниц и более точного UI.
- Статус: `accepted`

## 2026-04-18 - Sound Color Trainer MVP

- Что изменилось:
  - добавлен отдельный локальный модуль `звук -> цвет`;
  - реализованы экраны `старт`, `калибровка`, `тренировка`, `обратная проверка`, `статистика`;
  - добавлена генерация 12 нот через `Web Audio API`;
  - локальное хранение профиля и попыток вынесено в отдельный доменный модуль;
  - добавлены integration-тесты доменной логики и отдельный `docs/tests.md`.
- Зачем:
  - собрать минимальный рабочий инструмент устойчивых аудиовизуальных якорей без сервера и внешних API;
  - заложить структуру, которую можно потом расширять на интервалы и аккорды.
- Статус: `accepted`

## 2026-04-18 - Sound Color Trainer Color Refinement Patch

- Что изменилось:
  - в модуле `звук -> цвет` добавлен второй уровень выбора цвета через popover после клика по базовой палитре;
  - цветовая модель расширена с простого `HEX` до объекта `baseId + HEX + HSL`;
  - проверка совпадения в `звук -> цвет` перестала быть только exact-match и теперь допускает близкие оттенки внутри базового цвета;
  - сохранена совместимость со старыми локальными данными, где цвет был строкой `HEX`.
- Зачем:
  - убрать слишком грубый выбор цвета при числе нот больше числа базовых цветов;
  - сохранить текущий flow модуля, но дать пользователю точную настройку оттенка без полного спектра.
- Статус: `accepted`

## 2026-04-20 - Sound Color Standalone Prototype

- Что изменилось:
  - добавлен автономный файл [`standalone/sound-color-prototype.html`](../standalone/sound-color-prototype.html);
  - prototype повторяет основной flow `звук -> цвет` без Next.js-сервера: звук, базовая палитра, уточнение оттенка, подтверждение, обратная проверка и статистика;
  - localStorage и Web Audio API сохранены внутри standalone-версии.
- Зачем:
  - дать быстрый способ проверить сам продуктовый сценарий, даже если локальный `next dev`/`build` нестабилен;
  - отделить проверку UI/flow от проблем инфраструктурного запуска всего репозитория.
- Статус: `accepted`
